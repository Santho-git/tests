Prerequisites
  * Before running the tests, ensure the following are installed on your system:
  * Python 3.9 or above
  * Google Chrome browser
  * ChromeDriver (matching your Chrome version and available in PATH)
  * selenium

To run a test file:
  python tests/ui_tests.py
  python tests/api_tests.py
